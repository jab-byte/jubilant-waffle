# 南京中考招生系统 - 新华云独立部署指南

> 本指南适用于直接部署到新华云/阿里云 ECS，不经过 Vercel 转发

## 部署包内容

```
├── .next/standalone/    # Next.js 独立应用（包含 server.js）
├── .next/static/        # 静态资源文件
├── public/              # 公共资源
├── .env.production      # 环境变量（Supabase 配置）
├── package.json         # 项目配置
└── 本文档
```

---

## 一、服务器要求

- **操作系统**: CentOS 7+、Ubuntu 20.04+ 或 Debian 10+
- **内存**: 至少 1GB（推荐 2GB）
- **CPU**: 1 核+
- **硬盘**: 10GB+
- **端口**: 80、443、3000

---

## 二、一键部署脚本

将部署包上传到服务器后，运行以下命令：

```bash
# 1. 解压部署包
cd /home
tar -xzf 南京中考招生系统-独立部署包.tar.gz

# 2. 进入应用目录
cd zs-app

# 3. 运行一键部署脚本
chmod +x deploy.sh
./deploy.sh
```

---

## 三、手动部署步骤

### 3.1 安装 Node.js 18+

**Ubuntu/Debian:**
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

**CentOS:**
```bash
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs
```

验证安装:
```bash
node -v   # 应显示 v18.x.x 或更高
npm -v    # 应显示 9.x.x 或更高
```

### 3.2 安装 PM2（进程管理）

```bash
npm install -g pm2
```

### 3.3 部署应用

```bash
# 1. 创建应用目录
sudo mkdir -p /opt/zs-app
cd /opt/zs-app

# 2. 解压部署包（假设已上传到 /home）
sudo tar -xzf /home/南京中考招生系统-独立部署包.tar.gz -C /opt/zs-app --strip-components=1

# 3. 复制静态文件到 standalone 目录
cp -r .next/static .next/standalone/.next/
cp -r public .next/standalone/

# 4. 复制环境变量文件
cp .env.production .next/standalone/.env.local

# 5. 启动应用
cd .next/standalone
pm2 start server.js --name "zs-app" --env production

# 6. 设置开机自启
pm2 startup
pm2 save
```

### 3.4 配置 Nginx 反向代理

安装 Nginx:
```bash
# Ubuntu/Debian
sudo apt-get install -y nginx

# CentOS
sudo yum install -y nginx
```

创建配置文件:
```bash
sudo nano /etc/nginx/sites-available/zs-app
```

写入以下内容（将 `your-domain.com` 替换为您的域名或 IP）:
```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 静态文件缓存
    location /_next/static/ {
        alias /opt/zs-app/.next/standalone/.next/static/;
        expires 365d;
        access_log off;
    }

    location /public/ {
        alias /opt/zs-app/.next/standalone/public/;
        expires 30d;
    }

    # 代理到 Next.js
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

启用配置:
```bash
# Ubuntu/Debian
sudo ln -s /etc/nginx/sites-available/zs-app /etc/nginx/sites-enabled/
sudo rm /etc/nginx/sites-enabled/default

# CentOS (编辑 /etc/nginx/nginx.conf 添加 include)

# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
sudo systemctl enable nginx
```

### 3.5 配置防火墙

```bash
# Ubuntu (ufw)
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 3000/tcp
sudo ufw enable

# CentOS (firewalld)
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --permanent --add-port=3000/tcp
sudo firewall-cmd --reload
```

---

## 四、配置 HTTPS（推荐）

使用 Let's Encrypt 免费 SSL 证书:

```bash
# 安装 Certbot
sudo apt-get install -y certbot python3-certbot-nginx  # Ubuntu/Debian
# 或
sudo yum install -y certbot python3-certbot-nginx      # CentOS

# 获取证书（将 your-domain.com 替换为您的域名）
sudo certbot --nginx -d your-domain.com

# 自动续期测试
sudo certbot renew --dry-run
```

---

## 五、常用管理命令

```bash
# 查看应用状态
pm2 status

# 查看日志
pm2 logs zs-app

# 重启应用
pm2 restart zs-app

# 停止应用
pm2 stop zs-app

# 查看 Nginx 状态
sudo systemctl status nginx

# 查看 Nginx 日志
sudo tail -f /var/log/nginx/error.log
```

---

## 六、故障排查

### 应用无法访问

1. 检查应用是否运行:
   ```bash
   pm2 status
   ```

2. 检查端口监听:
   ```bash
   netstat -tlnp | grep 3000
   ```

3. 检查 Nginx 配置:
   ```bash
   sudo nginx -t
   ```

### 数据库连接失败

1. 检查环境变量:
   ```bash
   cat /opt/zs-app/.next/standalone/.env.local
   ```

2. 测试 Supabase 连接（在浏览器 Console 中查看）

### 403 Forbidden 错误

- 确保 Supabase RLS 已禁用（之前已处理）
- 检查 Nginx 配置中的 proxy_pass 是否正确

---

## 七、更新应用

```bash
# 1. 停止当前应用
pm2 stop zs-app

# 2. 备份旧版本
sudo mv /opt/zs-app /opt/zs-app-backup

# 3. 解压新版本
sudo mkdir -p /opt/zs-app
sudo tar -xzf /home/新版本部署包.tar.gz -C /opt/zs-app --strip-components=1

# 4. 复制静态文件
cd /opt/zs-app
cp -r .next/static .next/standalone/.next/
cp -r public .next/standalone/
cp .env.production .next/standalone/.env.local

# 5. 重启应用
cd .next/standalone
pm2 restart zs-app
```

---

## 八、环境变量说明

`.env.production` 包含:
```
NEXT_PUBLIC_SUPABASE_URL=https://ozdvmsosnycegpctldml.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=您的Supabase密钥
```

这些是客户端公开变量，用于连接 Supabase 数据库。

---

**部署日期**: 2026-05-08
**应用版本**: v9（含 Excel 导入功能）
