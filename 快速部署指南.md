# 🚀 快速部署到阿里云 ECS - 5分钟指南

## 📥 第一步：上传文件到 ECS

```bash
# 在你的本地电脑上运行
scp -i your-key.pem 南京中考招生系统-阿里云部署包.tar.gz root@your-ecs-ip:/home/
```

## 🔧 第二步：在 ECS 上部署

```bash
# SSH 连接到 ECS
ssh -i your-key.pem root@your-ecs-ip

# 解压
mkdir -p /opt/zs-app
tar -xzf /home/南京中考招生系统-阿里云部署包.tar.gz -C /opt/zs-app/
cd /opt/zs-app

# 安装依赖
npm install -g pnpm
pnpm install --prod
```

## ▶️ 第三步：启动应用

### 选项 A：直接运行（测试）
```bash
./start.sh
# 然后访问 http://your-ecs-ip:3000
```

### 选项 B：后台运行（推荐）
```bash
# 安装 PM2
npm install -g pm2

# 启动
pm2 start "NODE_ENV=production node .next/standalone/server.js" --name "zs-app"

# 开机自启
pm2 startup
pm2 save
```

## 🌐 第四步：配置 Nginx（可选）

```bash
# 创建 Nginx 配置
sudo tee /etc/nginx/sites-available/zs-app > /dev/null <<EOF
server {
    listen 80;
    server_name _;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade \$http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host \$host;
        proxy_cache_bypass \$http_upgrade;
    }
}
EOF

# 启用并重启
sudo ln -sf /etc/nginx/sites-available/zs-app /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

然后访问 `http://your-ecs-ip` 或 `http://your-domain.com`

## 🐛 常见问题

| 问题 | 解决方案 |
|-----|--------|
| Port 3000 already in use | `sudo lsof -i :3000` 找出进程，然后 `kill -9 <PID>` |
| Cannot find module | 确认运行了 `pnpm install --prod` |
| 无法访问 | 检查 ECS 安全组是否开放了 80/3000 端口 |
| Supabase 连接失败 | 检查 `.env.local` 中的 URL 和 Key 是否正确 |

## 📋 登录凭证

用户名: `admin`  
密码: `123456`

## 📞 需要帮助？

详细部署指南：查看 `部署到阿里云-步骤指南.md`

---

**状态**：✅ 已就绪部署  
**环境**：Node.js 18+, pnpm  
**大小**：~13MB  
**内存要求**：≥1GB
