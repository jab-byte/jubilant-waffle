# 南京中考招生系统 - 阿里云部署指南

## 📦 包内容

- `.next/` - Next.js 优化后的应用代码
- `public/` - 静态资源文件
- `package.json` - 项目依赖配置
- `next.config.mjs` - Next.js 配置
- `.env.local` - 环境变量（Supabase 连接信息）

## 🚀 部署方案（推荐）

由于应用使用 Supabase 数据库和实时功能，不能导出为纯静态 HTML，需要使用以下部署方案：

### **方案一：阿里云 ECS + Node.js（推荐）**

#### 前置条件
- 阿里云 ECS 实例（至少 1GB 内存）
- CentOS 7 或 Ubuntu 20.04+
- 已开放 80 和 443 端口

#### 部署步骤

1. **SSH 连接到 ECS**
   ```bash
   ssh -i your-key.pem root@your-ecs-ip
   ```

2. **安装 Node.js 和 pnpm**
   ```bash
   # 安装 Node.js 18+
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   
   # 安装 pnpm
   npm install -g pnpm
   ```

3. **上传部署包到 ECS**
   在本地运行：
   ```bash
   scp -i your-key.pem 南京中考招生系统-部署包.tar.gz root@your-ecs-ip:/home/
   ```

4. **在 ECS 上解压并启动**
   ```bash
   cd /home
   tar -xzf 南京中考招生系统-部署包.tar.gz -C /opt/zs-app/
   cd /opt/zs-app
   
   # 安装依赖
   pnpm install --prod
   
   # 启动应用
   NODE_ENV=production node .next/standalone/server.js
   ```
   应用会监听 3000 端口

5. **配置 Nginx 反向代理（可选但推荐）**
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;  # 替换为你的域名或 IP
       
       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           proxy_set_header X-Forwarded-Proto $scheme;
       }
   }
   ```
   重启 Nginx：
   ```bash
   sudo systemctl restart nginx
   ```

6. **配置 PM2 开机自启**
   ```bash
   # 安装 PM2
   pnpm install -g pm2
   
   # 启动应用
   pm2 start ".next/standalone/server.js" --name "zs-app" --env NODE_ENV=production
   
   # 设置开机自启
   pm2 startup
   pm2 save
   ```

---

### **方案二：阿里云 OSS（仅用于前端文件存储加速）**

> ⚠️ 注意：此方案仅适用于前端静态文件（CSS、JS、图片），应用本体仍需 Node.js 服务

1. 创建 OSS Bucket
2. 上传 `.next/static/` 文件夹到 OSS
3. 在 `next.config.js` 中配置 CDN 域名

---

### **方案三：使用 Vercel + 国内 CDN（过渡方案）**

> ✅ 适合短期使用，域名备案完成后可迁移

优点：
- 无需管理服务器
- 自动扩展、SSL 证书
- 只需配置 CDN 加速

步骤：
1. 应用继续部署在 Vercel（已完成）
2. 在阿里云 CDN 配置源站为 Vercel 域名
3. 配置你的备案域名指向 CDN

---

## ⚙️ 环境变量说明

`.env.local` 文件已包含：
```
NEXT_PUBLIC_SUPABASE_URL=https://ozdvmsosnycegpctldml.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

这些变量是**公开的**（NEXT_PUBLIC 前缀），用于客户端连接 Supabase。

---

## 🔐 安全建议

1. **定期备份数据库**
   - 在 Supabase 控制台设置自动备份

2. **启用 HTTPS**
   - 配置 Let's Encrypt SSL 证书（免费）
   - 在 ECS 上使用 Certbot：
     ```bash
     sudo apt-get install certbot python3-certbot-nginx
     sudo certbot certonly --nginx -d your-domain.com
     ```

3. **监控性能**
   - 使用 Supabase 控制台查看 API 调用量
   - 配置 CloudWatch 或阿里云监控

---

## 📊 故障排查

### 应用无法启动
```bash
# 查看日志
pm2 logs zs-app

# 或直接运行
NODE_ENV=production node .next/standalone/server.js
```

### 数据库连接失败
- 检查 `.env.local` 中的 Supabase URL 和 Key
- 确保 Supabase 项目运行中（在 Supabase 控制台检查）

### 域名备案完成后的迁移
- 更新 DNS 记录指向新的 ECS IP
- 无需修改应用代码

---

## 📞 技术支持

- **Supabase 问题**：https://supabase.com/docs
- **Next.js 文档**：https://nextjs.org/docs
- **阿里云 ECS**：https://help.aliyun.com/

---

**部署日期**：2026-04-20  
**应用版本**：v1.0（含沟通日志时间线、附件预览）
